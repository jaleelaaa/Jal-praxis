# Environment Variables for Render Deployment
# Copy this file to .env.local for local development
# These will be configured in Render's environment settings

# Node environment
NODE_ENV=production

# Port (Render will automatically set this)
PORT=3000

# Optional: Custom domain if you have one
# NEXT_PUBLIC_SITE_URL=https://your-custom-domain.com

# Optional: Analytics or monitoring
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Optional: API keys if your documentation integrates with external services
# API_KEY=your-api-key